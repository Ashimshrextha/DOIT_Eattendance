namespace AttendanceManagementSystem
{
    public static class UnityConfig
    {
        public static void RegisterComponents()
        {
            //var container = new UnityContainer();
            // e.g. container.RegisterType<ITestService, TestService>();
            //container.RegisterType<EAttendanceSystemDBEntities>(new PerResolveLifetimeManager());
            //container.RegisterType<IUnitOfWork, UnitOfWork>();
           // container.RegisterType(typeof(IBaseRepository<TEntity, TModel>), typeof(BaseRepository<>));

            //container.RegisterType<ISystemMenuServices, SystemMenuServices>();

            //DependencyResolver.SetResolver(new UnityDependencyResolver(container));
        }
    }
}